<h3 class="text-center mt-4"><%=@category.name%>'s Articles are </h3>

<div class="text-center mt-4 ">
 <% if (loggedin? && current_user.admin?)%>
 <%=link_to "Edit Category ",edit_category_path(@category),class:"btn btn-outline-success " %>
 </div>
 <%end%>  
<div class="flickr_pagination">
 <%= will_paginate @articles, :container => false %> 
  </div> 
<%= render 'articles/article',obj:@category.articles%>
